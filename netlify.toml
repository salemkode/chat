[build]
  command = "bun run build && cp public/_redirects dist/_redirects"
  publish = "dist"

[build.environment]
  BUN_VERSION = "1.3.9"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
